# Paper Workspace

- `manuscript/`: current working manuscript source files (`.Rmd`, `.tex`)
- `versions/`: dated frozen snapshots (`YYYY-MM-DD_vX`)
- `submissions/`: journal-specific submission packages
- `figures/`: manuscript figure sources

Build artifacts (`.pdf` and LaTeX generated files) are ignored by `.gitignore`.
